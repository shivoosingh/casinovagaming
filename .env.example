NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Google OAuth (Continue with Google) ─────────────────────────────────────
#
# A) Google Cloud Console (https://console.cloud.google.com/apis/credentials)
#    1. Create OAuth 2.0 Client ID → Web application
#    2. Authorized JavaScript origins:
#         http://localhost:3000
#         https://YOUR-PRODUCTION-DOMAIN.com
#    3. Authorized redirect URIs (Supabase handles Google callback):
#         https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback
#
# B) Supabase Dashboard → Authentication → Providers → Google
#    - Enable Google
#    - Paste Client ID + Client Secret from Google Cloud
#
# C) Supabase Dashboard → Authentication → URL Configuration
#    - Site URL: https://YOUR-PRODUCTION-DOMAIN.com  (or http://localhost:3000 for dev)
#    - Redirect URLs (add ALL of these):
#         http://localhost:3000/auth/callback
#         https://YOUR-PRODUCTION-DOMAIN.com/auth/callback
#
# D) Vercel env vars: same NEXT_PUBLIC_SUPABASE_* + NEXT_PUBLIC_SITE_URL
#
# Email OTP (Phone / Email tab — codes sent to inbox, no Twilio SMS needed):
#   Supabase → Authentication → Providers → Email → Enable Email OTP
#   Optional: configure custom SMTP for production deliverability
#
# Phone login lookup (server resolves phone → email):
#   Add service role key below (Settings → API → service_role — never expose to browser)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ── AI blog drafts (Admin → CMS → Generate post) ────────────────────────────
# Free key: https://aistudio.google.com/apikey
# Optional model override (default gemini-2.5-flash). Cover images use free Pollinations.
GEMINI_API_KEY=your_gemini_api_key
# GEMINI_MODEL=gemini-2.5-flash
# Optional LLM fallbacks for AI Auto Blog / Chatbot (if Gemini is blocked)
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
# GROQ_API_KEY=
# Optional: profile UUID used as AI chat message sender (defaults to first admin)
# CASINOVA_BOT_SENDER_ID=

# Cron jobs (/api/cron/*) — Vercel Cron or external scheduler
# CRON_SECRET=long_random_string

# NOWPayments (crypto deposits) — optional
# NOWPAYMENTS_API_KEY=
# NOWPAYMENTS_IPN_SECRET=

# Game worker remote control secret (optional)
# GAME_AUTOMATION_SECRET=

# ── Telegram admin alerts (offline customer messages) ───────────────────────
# Option A — private (only you): your numeric user id from getUpdates
# Option B — group (you + team): create a group, add the bot, post "hi", getUpdates
#   Group ids look like -1001234567890 (negative number)
# Multiple recipients: comma-separated, e.g. -1001234567890,5293095037
# 1. Message @BotFather → /newbot → copy bot token
# 2. Run supabase/admin-presence.sql in Supabase SQL Editor
# 3. Add vars to Vercel Production and redeploy
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_ADMIN_CHAT_ID=-1001234567890
# Optional promo channel for autopilot / marketing posts
# TELEGRAM_PROMO_CHAT_ID=

# Email (Resend SMTP → Supabase) — spinoracasinos.com
# 1. Resend → Domains → spinoracasinos.com verified (DNS in Cloudflare)
# 2. Resend → API Keys → create key (re_xxxxxxxx) — use ONLY in Supabase SMTP, never in frontend
# 3. Supabase → Project Settings → Authentication → SMTP Settings:
#      Enable Custom SMTP: ON
#      Host: smtp.resend.com
#      Port: 465
#      Username: resend
#      Password: <your Resend API key>
#      Sender email: noreply@spinoracasinos.com
#      Sender name: Spinora
# 4. Supabase → Authentication → Providers → Email:
#      Confirm email: ON  (required — no login until user clicks email link)
# 5. Supabase → Authentication → URL Configuration:
#      Site URL: https://spinoracasinos.com
#      Redirect URLs: https://spinoracasinos.com/auth/callback
#                     http://localhost:3000/auth/callback

# Deposit payment usernames (shown on game pages)
NEXT_PUBLIC_DEPOSIT_PAYPAL=@AnthonyCastro909
NEXT_PUBLIC_DEPOSIT_CHIME=$Anthony-Castro-208
NEXT_PUBLIC_DEPOSIT_CASHAPP=$AnthonyCastro80
NEXT_PUBLIC_DEPOSIT_BITCOIN=1L2GidwNBzXKcnrpdHYaVGpZSrhvQcSbKV
NEXT_PUBLIC_DEPOSIT_USDT=0x3d3528b297f150e0749f10bd91dae18e4defca45
NEXT_PUBLIC_DEPOSIT_VENMO=@Anthony-Castro-414

# Paydora Payment API — https://paydora.net/
# Webhook URL: https://www.casinovasgaming.com/api/payments/paydora/webhook
PAYDORA_API_BASE_URL=https://cpcs.paydora.net/api/v1
PAYDORA_API_KEY=pk_your_key
# Shown once when you add the webhook on the Paydora Webhooks tab
PAYDORA_WEBHOOK_SECRET=whsec_your_secret



# Social media links (footer)
NEXT_PUBLIC_TELEGRAM_URL=https://t.me/yourusername
NEXT_PUBLIC_FACEBOOK_URL=https://facebook.com/yourpage
NEXT_PUBLIC_WHATSAPP_URL=https://wa.me/1234567890
NEXT_PUBLIC_INSTAGRAM_URL=https://instagram.com/yourusername

# ── Juwa wallet-load automation (server-only — never NEXT_PUBLIC) ─────────────
# Agent panel URL you use today to create users + load credits
JUWA_ADMIN_URL=https://ht.juwa777.com/login
# Bot worker (workers/juwa-bot/.env) uses agent login + SUPABASE_SERVICE_ROLE_KEY

# ── Fire Kirin Terminal API (same family as Orion Stars / Milky Way) ─────────
# ORIONSTARS_API_URL=https://34.212.168.0:8033/ws/service.ashx
# ORIONSTARS_AGENT_USERNAME=Darklord1121
# ORIONSTARS_AGENT_PASSWORD=Re3set@123#
# Uses GAMEVAULT_PROXY_URL for egress if ORIONSTARS_PROXY_URL is not set

# MILKYWAY_API_URL=https://47.252.40.52:8033/ws/service.ashx
# MILKYWAY_AGENT_USERNAME=Darklord1121
# MILKYWAY_AGENT_PASSWORD=Darklord1121
# MILKYWAY_PROXY_URL=http://user:pass@your-proxy-ip:port

# FIREKIRIN_API_URL=https://52.41.26.140:8034/ws/service.ashx
# FIREKIRIN_ADMIN_URL=http://start.firekirin.xyz:8580/
# FIREKIRIN_AGENT_USERNAME=Darklord1121
# FIREKIRIN_AGENT_PASSWORD=Darklord1121
# FIREKIRIN_PROXY_URL=http://user:pass@your-proxy-ip:port
